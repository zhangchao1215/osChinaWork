<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="jiyun.com.oschinawork">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.WebActivity" />
        <activity android:name=".activity.EditTextActivity" />
        <activity android:name=".activity.MainActivity"></activity>
        <!--<activity android:name=".fragment.searchFragment.MainSearchFragment"/>-->
        <activity android:name=".fragment.searchFragment.Search_FromActivity"></activity>
        <activity android:name=".activity.BlogWebActivity"></activity>
        <activity android:name=".fragment.dongtan.TweetPupActivity"></activity>
        <activity android:name=".activity.HuoDongWebView"></activity>
        <activity android:name=".activity.TweetDeatil"></activity>
        <activity android:name=".activity.LoginActivity"></activity>
    </application>

</manifest>